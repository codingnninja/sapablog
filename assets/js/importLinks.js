const matter = 'https://cdn.jsdelivr.net/npm/front-matter@4.0.2/+esm';
const spaceTransformer = 'https://cdn.jsdelivr.net/npm/@shikijs/transformers@1.21.0/+esm';

const shiki = 'https://esm.sh/shiki@1.0.0';
const htmlParsers = '../utils/htmlParsers.js';

const links = {
  matter,
  spaceTransformer,
  shiki,
  htmlParsers
}

export default links;